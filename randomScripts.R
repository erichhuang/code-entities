# randomScripts.R

# SAVE CODE ENTITY ONE "GGHEAT2"
ggheat2Layer <- Code(list(name = "ggheat2.R",
                                parentId = 274060))
ggheat2Layer <- createEntity(ggheat2Layer)
ggheat2Layer <- addFile(ggheat2Layer, "ggheat2.R")
ggheat2Layer <- storeEntity(ggheat2Layer)
